providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm ci --omit=dev || cd backend && npm install --omit=dev"]

[start]
cmd = "cd backend && npm start"
